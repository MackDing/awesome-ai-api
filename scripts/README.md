# Scripts

Utility scripts for maintaining the gateway database.

## Planned

- `check-uptime.py` — Ping each gateway's public endpoint; produce uptime report
- `benchmark.py` — Run latency/throughput benchmarks across a fixed prompt set
- `sync-pricing.py` — Scrape public pricing pages and diff against `data/gateways.json`

PRs welcome!
